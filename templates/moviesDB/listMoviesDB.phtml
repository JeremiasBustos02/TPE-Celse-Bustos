<div class="col-md-8">
    <div class="genres mt-3">
        <h3>Genre List</h3>
        <ul class="list-group mb-4 w-100">
            <?php foreach ($movies as $movie): ?>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <p class="m-0"><?= htmlspecialchars($movie->title) ?></p>
                    <span>
                        <a href="modifyMovie/<?= $movie->id ?>" type="button"
                            class='btn btn-warning btn-sm m-1'>Modify</button>
                            <a href="removeMovie/<?= $movie->id ?>" type="button"
                                class='btn btn-danger btn-sm m-1'>Delete</a>
                    </span>
                </li>
            <?php endforeach ?>
        </ul>
    </div>
</div>